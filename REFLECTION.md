> AI4SE 期末项目反思报告 | 2026-08-10
## 1. Superpowers 技能：哪些有用，哪些流于形式
本项目中发挥作用最大的是 brainstorming 技能。它将系统设计拆解为六个维度逐一追问，我以为是采用问答形式，要深入学习和思考，但是实际使用起来其实是做选择题，我依靠自己已有的专业知识把选项发给其他大模型后权衡利弊做了选择。实话说 brainstorming 并没有强迫我把每个细节都想清楚，但是的确能形成一份不会有大改动的文档，也让我对于一些边缘情况在一开始就有数了，对于小项目的落地很有帮助（对比以前修修改改，缝缝补补的课设来说）。同时由于 AI 在不断追问，打磨，形成项目的时候，会出现 Token 累计导致的高昂价格和记忆失真，代码质量下降。先有全局观落地文档是有必要的。
相对流于形式的是 subagent-driven-development 工作流。理想情况下每个 task 应该启动一个全新的无状态 subagent，但实际操作中 AI 在一次会话内连续完成了所有 task，上下文并未真正隔离。这让"subagent 驱动"更像是批量代码生成，而不是课程设想的那种严格隔离的多 agent 协作。

## 2. TDD 在 AI 协作下：阻碍还是放大器
整体来说是放大器。AI 生成代码时自然地遵循了测试先行的结构——每个模块都配套了完整的 pytest 测试，且测试覆盖了边界情况（如 MockLM 队列耗尽、guardrail BLOCK 优先级高于 HITL 等）。
但我也观察到一个问题：AI 有时会写出"为通过测试而设计"的实现，而不是"为正确性而设计"。例如 classifier 的 UNKNOWN 类型处理，AI 写的实现在测试里能过，但逻辑上对空 stderr 的处理方式与 SPEC 的描述有细微出入。这提醒我评审代码时不能只看测试是否通过，还要对照 SPEC 逐条核查。未来我也许会考虑多 agent 协作，形成评审环（其实我一开始是考虑做评审 Harness 的）。

## 3. Subagent 工作流：能自主运行多久
核心模块的生成过程基本顺畅，AI 能连续推进而不脱轨，这得益于 SPEC 对每个模块的接口和行为描述得足够具体。
偏离最严重的阶段是容器部署。从第一次 push 到 Render 成功部署，共经历了 7 次 fix commit：inotify 实例限制、端口未动态绑定、Python 模块路径找不到、openai SDK 与 httpx 版本冲突、模型名大小写错误。这些问题都源于环境依赖，SPEC 没有预见到，AI 也没有主动提示。这说明 AI 对"代码正确性"的判断能力远强于对"环境兼容性"的预判能力。如果时间允许，我会尝试对比多种模型，看看他们对这种细节有没有梯度的把控。

## 4. 最优 task 颗粒度
PLAN 里以"一个模块文件"为颗粒度（如 Task 4 对应 classifier.py + test_classifier.py）是合适的。这个粒度让 AI 在一次会话内能完整实现一个有明确边界的功能，不会因为上下文过长而产生漂移，commit 历史也足够清晰。
如果颗粒度更粗（如把 classifier + guardrail 合并为一个 task），AI 倾向于在后半段偷懒或遗漏细节（之前读过一篇文献，说 AI 对首部和尾部的需求更关注，如果上下文太长会让结果更粗糙）；更细（如把 classify() 和 get_repairprompt() 分为两个 task），则 commit 过于碎片化，且两个函数强耦合，分开实现反而增加了接口不一致的风险。

## 5. SPEC/PLAN 质量对实现的影响
**正面案例：** SPEC 第3.5节对护栏三态决策（BLOCK/HITL_REQUIRED/ALLOW）的描述精确到了正则匹配的粒度，AI 生成的 guardrail.py 与设计高度一致，19 个单元测试一次全过。测试了豆包，gemini，chatgpt 对 task1 的复现，他们没有做追问，并自称能完成所有测试。
**负面案例：** SPEC 的风险章节没有提到 Streamlit 在 Linux 容器里的 inotify 限制。导致部署时连续出现三次相关错误（`inotify instance limit reached`），分别尝试了命令行参数、配置文件、降级版本三种方案才解决。如果 SPEC 的技术选型节里注明了"需在 Dockerfile 中禁用 Streamlit 文件监听"，可以节省至少一个小时。
这个案例说明：SPEC 的价值不只在于描述功能，还在于预判部署环境的约束。纯功能性的规约是不完整的。

## 6. 最有效的 prompt/context 策略
最有效的策略是逐节签字确认：brainstorming 每提出一个设计节，我明确给出"确认/修改+原因"，再进入下一节。这样 AI 的输出始终在我的期望范围内，不会积累误解到最后才发现方向偏了。相对低效的是我又新开了一个 agent 用于辅助我完成作业，包括理解作业意图，规划完成顺序，提醒我记录什么过程，以及中间环境出错帮我配置，还有后期检查是否完成所有目标。这样长的上下文积累导致后期给我的结果质量直线下降，我一看 token 用量倒是使劲涨。
## 7. 凭据与分发要求的价值
在实现 keyring + .env 双轨方案时，我意识到"本地开发环境"和"容器运行环境"对凭据访问的方式完全不同。本地可以用系统钥匙串，但容器里没有交互式桌面，keyring 会抛异常而非返回 None——这个细节如果没有作业要求认真对待凭据，我根本不会去想。
分发要求同样有价值：它迫使我真正验证了"在一台全新机器上能否运行"，而不是停留在"在我的电脑上能跑"的假设。这两条要求把很多工程师习惯忽略的"最后一公里"问题显式化了。

## 8. 如果重做会改变什么
首先，会在 SPEC 风险章节专门列一条"容器部署环境约束"，把 Streamlit fileWatcherType、PYTHONPATH、端口动态绑定等问题提前记录。
其次，会在本地先用 Docker 验证镜像可以正常启动，再推送到 Render，而不是直接用 Render 的远程构建日志调试。本地 Docker 的反馈循环比等待 Render 部署快 5 倍以上。
最后，会为 CI 单独写一个冒烟测试，确保每次 push 都验证了核心机制的端到端行为，而不只是单元测试。

## 9. 对 Superpowers 方法论的批判
Superpowers 的核心假设是：清晰的 SPEC 和 TDD 纪律能让 AI agent 长时间自主推进而不偏离。这个假设在"功能代码"领域基本成立，但在"环境配置"领域几乎不成立——没有任何 SPEC 能预见所有的依赖冲突和平台差异。
方法论另一个隐含假设是开发者有充足时间走完七步流程。但是我给自己留的时间比较短，AGENT_LOG 和 SPEC_PROCESS 的维护会成为负担，容易变成事后补写而非实时记录，失去了"过程证据"的价值。如果有充足时间的情况下，我再做一次这个作业，我会考虑好好利用 AGENT_LOG 和 SPEC_PROCESS 。
